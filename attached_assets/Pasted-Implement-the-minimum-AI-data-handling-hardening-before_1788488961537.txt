Implement the minimum AI/data-handling hardening before real institutional/customer documents are used. Keep the scope narrow and preserve current Kontra behavior.
Set store: false explicitly on all OpenAI Chat Completions calls used by customer-facing Deal Room/document/Ask flows. Centralize this where practical rather than relying on developers to remember it per call.
Stop sending complete Base64 scanned PDFs through the normal Deal Room fallback. Reuse the existing rendered-page approach already implemented in aiDealReview. Preserve document-analysis quality and establish controlled page/size limits. Do not silently analyze only four pages if that could materially omit transaction evidence.
Remove sensitive content from broad application/audit logging. Do not log raw document text, AI prompts, source excerpts, request bodies containing customer content, SSNs/EINs, or model outputs. Preserve operational metadata needed for debugging: route, tenant/room identifier, authenticated user identifier, status, latency, model, request ID and safe byte/count metadata.
Production encryption must fail closed. Remove deterministic fallback encryption-key behavior for production. If required encryption keys are absent or invalid, sensitive logging/storage requiring those keys must not start or silently use a predictable fallback. Preserve safe local-development behavior separately if needed.
Make institutional Deal Room AI routing explicit. Customer-document processing and room-grounded Kontra AI must use the approved OpenAI provider/project path and must not silently fall through to Azure, Anthropic, Ollama, custom OpenAI-compatible endpoints, or other providers. Do not remove those providers if other legacy/demo features need them; isolate the institutional Deal Room path.
Do not send SSNs or similarly sensitive identity numbers to the general model merely for automatic extraction. Trace the current underwriting SSN/EIN extraction path and either extract sensitive identifiers locally, require manual entry, or exclude them from model submission. Do not break unrelated underwriting fields.
Harden AI/provider error handling so raw provider errors, prompts, document fragments, or response content are not returned to clients or written to unsafe logs.
If Sentry is enabled, explicitly scrub request bodies, query strings, authorization/cookie headers, document/file content, AI prompts/responses and sensitive transaction data before transmission. Do not enable Sentry merely for this task.
Do not implement ZDR/MAM or regional OpenAI routing in code. Those require separate OpenAI organization/project/account confirmation. Do not change models or prompts except where required for the scanned-PDF handling and sensitive-data exclusion above.
Preserve DOCX/PDF extraction, Transaction Record hydration, provenance, cross-document verification, Kontra AI grounding, Verified Asset, Digital Asset Readiness and document View/Download behavior.
Add focused regression/security tests for each change and report exactly what changed, what remained unchanged, and anything requiring an external OpenAI account setting.