Revision 10 is still visually off. Please perform a complete branding/layout pass rather than another incremental formatting adjustment.
1. Add the official Kontra logo
Add the Kontra logo to the upper-left header of every page.
Replace the current text-only KONTRA / DIGITAL ASSET PREPARATION treatment with the logo plus a subtle DIGITAL ASSET PREPARATION label.
Keep the logo professional and understated; this should resemble an institutional verification/reporting artifact, not marketing material.
2. Remove the burgundy/red section-heading color
The burgundy headings currently conflict visually with the navy table headers and teal Kontra accent.
Use Kontra teal for primary section headings.
Use dark navy/charcoal for table headers and primary text.
Use light neutral gray for alternating rows/backgrounds.
Reserve red only for actual blockers, exceptions, failed verification states, or warnings.
3. Create one consistent Kontra design system
Kontra teal = brand/accent/section headings
Dark navy/charcoal = table headers and primary text
Neutral gray = secondary text, borders and alternating rows
White = main background
Status colors should be semantic: green/teal for verified/eligible, amber for pending, red for blocker/failed.
4. Fix the page-width/layout bug completely
Section headings must NEVER be placed in a narrow right-side column.
Readiness, approvals, blockers and exceptions, Approvals and review record, Provider-neutral disclaimer, Evidence reference register, etc. must span the normal document content width.
Never allow words to wrap character-by-character.
A section heading should either remain with its content or move completely to the next page.
The disclaimer must render as a normal full-width section, not a narrow vertical column.
5. Improve visual hierarchy
Logo/header
Document title
Short description
Section heading
Table/content
Supporting/technical information
Do not give every element equal visual weight. UUIDs, hashes and technical references should be visually secondary.
6. Make tables feel like part of the same Kontra design
Keep consistent header background, typography, border weight, padding and row height across every table.
Increase whitespace/padding slightly.
Avoid overly dense database-export styling.
Long hashes/IDs should use a smaller secondary font and wrap gracefully.
7. Preserve all underlying package data, hashes, immutable snapshot architecture, provenance and audit records. This request is presentation-layer only.
The target should feel like an institutional document generated by Kontra, comparable in visual discipline to a professional diligence, verification or transaction report—not a raw database export converted to PDF.